# install_auto_graphify
graphify Auto-Installer — a one-click PowerShell script that turns any codebase into an interactive knowledge graph and wires it into Claude Code. Fully automatic: admin elevation, Python and graphify install, project linking, then it opens the map and a user guide in your browser and launches Claude. Drop it in a project, run it, done.
